global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ml-api"

    static_configs:
      - targets: ["host.docker.internal:5000"]

  - job_name: "system-exporter"

    static_configs:
      - targets: ["host.docker.internal:8000"]
